/*globals require:true */
/*jslint white:true */
(function(window) {
	"use strict";
	var
		  F        = window.Sluraff
		, $        = window.jQuery
		, _        = window._
		, Backbone = window.Backbone
	;

	F.defs.Feature = Backbone.Model.extend({
		initialize: function() {

		},
		sync: function() {}
	});
}(this));